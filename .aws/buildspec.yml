version: 0.2

phases:
  build:
    commands:
      - echo Build Starting on `date`
      - gradle build -x test
  post_build:
    commands:
      - echo $(basename ./build/libs/*.jar)
      - pwd

artifacts:
  secondary-artifacts:
    elements-auth:
      discard-paths: yes
      base-directory: elements-auth
      files:
        - build/libs/*.jar
    elements-api:
      discard-paths: yes
      base-directory: elements-api
      files:
        - build/libs/*.jar
    game-session:
      discard-paths: yes
      base-directory: game-session
      files:
        - build/libs/*.jar
