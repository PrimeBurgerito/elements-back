#!/usr/bin/env groovy

pipeline {
    agent {
        docker {
            image 'openjdk:13-slim'
            args '-v /var/tmp/app:/var/lib/jenkins/workspace/elements-back/build -u root'
        }
    }

    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                sh 'ls -a'
                sh 'chmod +x gradlew'
                sh './gradlew assemble'
                sh 'sudo mv -v elements-api/build/libs/*.jar  /var/lib/jenkins/workspace/elements-back/build/elements-api.jar'
                sh 'sudo mv -v game-session/build/libs/*.jar /var/lib/jenkins/workspace/elements-back/build/elements-session.jar'
            }
        }
    }
}
