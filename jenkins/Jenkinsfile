#!/usr/bin/env groovy

pipeline {
    agent {
        docker {
            image 'openjdk:13-slim'
            args '-v /var/tmp/app:/tmp'
        }
    }

    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                sh 'ls -a'
                sh 'chmod +x gradlew'
                sh './gradlew clean assemble'
                sh 'mv -v elements-api/build/libs/*.jar  /tmp/elements-api.jar'
                sh 'mv -v game-session/build/libs/*.jar /tmp/elements-session.jar'
            }
        }
    }
}
