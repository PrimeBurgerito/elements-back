#!/usr/bin/env groovy

pipeline {
    agent {
        docker {
            image 'openjdk:13-slim'
            args '-v /tmp:/tmp'
        }
    }

    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                sh 'ls -a'
                sh 'chmod +x gradlew'
                sh './gradlew clean assemble'
                sh 'mv -v elements-api/build/libs/*.jar /var/tmp/app/elements-api.jar'
                sh 'mv -v game-session/build/libs/*.jar /var/tmp/app/elements-session.jar'
            }
        }
    }
}
