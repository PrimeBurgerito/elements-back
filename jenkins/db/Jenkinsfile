#!/usr/bin/env groovy

pipeline {
    agent any

    stages {
        stage('Start MongoDB') {
            steps {
                def files = findFiles()

                files.each { f ->
                    if (f.directory) {
                        echo "This is directory: ${f.name} "
                    }
                }
            }
        }
    }
}
